document how to use grep to revert back to the older .calendar
format, removing all the LastModified lines.
use libical to parse incoming iCalendar test
https://github.com/libical/libical/blob/4.0/docs/UsingLibical.md

add copy item as ics to clipboard / paste item as ics from clipboard

allow iCalendar format when creating items from stdin or clipboard
the clipboard

ical2pdf could make adjustments to force the appointments
to fit into the box for the day.
    remove internal blank lines
    if there is no left header
        wrap the first line into the cell header space
    shrink the font

Allow iso8601 dates and times when parsing input data.
